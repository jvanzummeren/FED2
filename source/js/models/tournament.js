// Part of Tournaments collection
// Adds no new functionality to the backbone model
define([

], function(){
  var TournamentModel = Backbone.Model.extend({
  });
  
  // Return the model for the module
  return TournamentModel;
});